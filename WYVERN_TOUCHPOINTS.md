# Upstream touchpoints

Files owned by upstream that Wyvern modifies. Keep this list short — every entry is a future
merge conflict. Anything Wyvern adds should be a new file, ideally under `wyvern/`.

| File | Why |
|---|---|
| _(none)_ | the Go source is upstream `v1.0.0-beta29`, unmodified |

Wyvern-owned files, which upstream will never touch: `README.md`, `NOTICE.md`,
`WYVERN_TOUCHPOINTS.md`, `.gitattributes`, and the `## v1.0.0-beta29` entry in `CHANGELOG.md`.
